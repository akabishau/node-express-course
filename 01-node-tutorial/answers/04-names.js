// local values
const doNotShareValue = "secret value"

// shared values
const someSharedValue = `shared value from ${module.filename}`
const aleksey = 'Aleksey'

module.exports = { someSharedValue, aleksey }